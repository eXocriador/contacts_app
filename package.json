{
  "name": "contacts-app-root",
  "version": "1.0.0",
  "description": "Root package.json for Contacts App with Frontend and Backend",
  "main": "index.js",
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "dev": "concurrently \"yarn --cwd frontend dev\" \"yarn --cwd backend dev\"",
    "dev:frontend": "yarn --cwd frontend dev",
    "dev:backend": "yarn --cwd backend dev",
    "install:all": "yarn install && yarn install --cwd frontend && yarn install --cwd backend",
    "build:all": "yarn build --cwd frontend && yarn build --cwd backend",
    "start:all": "concurrently \"yarn --cwd frontend start\" \"yarn --cwd backend start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
